// import ClientForm from './client-form';
import AddClient from './add-client';
import EditClient from './edit-client';

console.log({ bla: AddClient, bla2: EditClient });

export default {
    // ClientForm,
    AddClient,
    EditClient,
}